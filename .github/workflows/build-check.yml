name: build-check

on:
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build

  rls-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - run: >
          supabase start
          -x analytics -x edge-runtime -x functions -x imgproxy -x inbucket
          -x kong -x meta -x realtime -x rest -x storage -x studio -x vector
      - run: supabase test db supabase/tests/database
